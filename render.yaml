# Render configuration for Static Site
static_sites:
  - name: character-ai
    buildCommand: npm run build
    staticPath: "./dist"
    routes:
      - type: rewrite
        source: "/*"
        destination: "/index.html"
    env:
      - key: NODE_VERSION
        value: 18
    buildCommand: |
      npm ci
      npm run build
